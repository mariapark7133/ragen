<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TEST">

    <select id="selectTestInfoList" resultType="exampleDTO">
        SELECT
            SEQ
            ,TITLE
            ,CONTENTS
        FROM TEST_INFO
    </select>

    <select id="selectQueryList" resultType="HashMap" parameterType="HashMap">
        ${query}
    </select>
</mapper>
